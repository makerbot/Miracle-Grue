#ifndef CONFIG_H
#define CONFIG_H

//
// Our basic numerical type. Float for now; we should switch
// to fixed-point math at some point. We may even want to use
// floating until we get to the slice point; who knows what scale
// the input files will be.
//

typedef float Scalar;

#endif // CONFIG_H

